---
phase: 01-dashboard-overview-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/admin/stats/route.ts
  - src/components/admin/stat-card-enhanced.tsx
  - src/app/(dashboard)/admin/page.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Dashboard displays 4 animated stat cards with counting animation"
    - "Each stat card shows mini sparkline chart representing last 7 days"
    - "Each stat card shows percentage change from previous period"
    - "Stats grid displays 4 columns on desktop, 2 on tablet, 1 on mobile"
    - "Stats API responds with cached data (300s revalidation)"
  artifacts:
    - path: "src/components/admin/stat-card-enhanced.tsx"
      provides: "Animated stat card with sparkline and trend indicator"
      min_lines: 80
    - path: "src/app/api/admin/stats/route.ts"
      provides: "Enhanced stats endpoint with trend data and caching"
      exports: ["GET"]
  key_links:
    - from: "src/app/(dashboard)/admin/page.tsx"
      to: "src/components/admin/stat-card-enhanced.tsx"
      via: "import StatCardEnhanced"
      pattern: "import.*StatCardEnhanced"
    - from: "src/app/(dashboard)/admin/page.tsx"
      to: "/api/admin/stats"
      via: "fetch with revalidate"
      pattern: "fetch.*api/admin/stats.*revalidate"
---

<objective>
Create animated dashboard stat cards with sparklines, trend indicators, and server-side caching.

Purpose: Transform static number cards into engaging, informative widgets that show trend context at a glance while establishing the code-split foundation for heavy chart components in Plan 02.

Output: StatCardEnhanced component with Framer Motion animation, mini sparklines (pure SVG, no library), percentage change badges, and cached stats API endpoint.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase work (Phase 0 established patterns)
# Stats API already optimized with single aggregation query
# Framer Motion already installed in project

@src/app/api/admin/stats/route.ts
@src/components/admin/stat-card.tsx
@src/app/(dashboard)/admin/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend stats API with trend data and caching</name>
  <files>src/app/api/admin/stats/route.ts</files>
  <action>
Extend the existing stats API (already optimized in Phase 0) to include trend data:

1. Add time-series data for sparklines (last 7 days counts per entity):
   - Use PostgreSQL date_trunc and generate_series for consistent 7-day buckets
   - Return as `sparkline: number[]` array (7 values, oldest to newest)

2. Add percentage change calculation:
   - Compare current period (last 7 days) vs previous period (7-14 days ago)
   - Return as `change: { value: number, isPositive: boolean }`

3. Apply Next.js fetch caching:
   - Export `revalidate = 300` (5 minutes)
   - DO NOT use `dynamic = 'force-dynamic'` - let Next.js cache

Response shape (extend existing):
```typescript
{
  newsCount: number,
  newsSparkline: number[],      // NEW: [d-7, d-6, d-5, d-4, d-3, d-2, d-1]
  newsChange: { value: number, isPositive: boolean }, // NEW
  documentsCount: number,
  documentsSparkline: number[],
  documentsChange: { value: number, isPositive: boolean },
  // ... same pattern for publishedNews, downloads, events
}
```

Use single $queryRaw with subqueries (follow Phase 0 pattern) to get all trend data in one query.

AVOID: Multiple round trips to database. The existing single-query pattern MUST be preserved.
  </action>
  <verify>
`curl -s http://localhost:3017/api/admin/stats | jq '.newsSparkline | length'` returns 7
`curl -s http://localhost:3017/api/admin/stats | jq '.newsChange'` returns object with value and isPositive
  </verify>
  <done>Stats API returns sparkline arrays (7 values each) and change objects for all 4 stat types</done>
</task>

<task type="auto">
  <name>Task 2: Create StatCardEnhanced component with animation and sparklines</name>
  <files>src/components/admin/stat-card-enhanced.tsx</files>
  <action>
Create new enhanced stat card component (do NOT modify existing stat-card.tsx):

1. Counting animation with Framer Motion:
   - Animate from 0 to final value over 1.5s
   - Use useMotionValue + useTransform + animate
   - Round to integer during animation

2. Mini sparkline chart (pure SVG, NO external library):
   - 70px wide x 24px tall
   - Line chart with gradient fill below
   - Color matches card theme (emerald/blue/purple/orange)
   - Normalize values to fit height (min-max scaling)

3. Trend indicator badge:
   - Arrow up/down icon with percentage
   - Green badge for positive, red for negative
   - Position: bottom right of card

4. Keep existing visual design from stat-card.tsx:
   - Gradient background
   - Icon in top right
   - Responsive sizing

Props interface:
```typescript
interface StatCardEnhancedProps {
  title: string;
  value: number;
  icon: LucideIcon;
  color: "emerald" | "blue" | "purple" | "orange";
  sparkline: number[];           // 7 values for mini chart
  change: { value: number; isPositive: boolean };
}
```

AVOID: Installing recharts or any chart library for sparklines - use pure SVG polyline with path.
AVOID: Heavy animations that cause layout shift - use opacity and transform only.

Add "use client" directive since Framer Motion requires client component.
  </action>
  <verify>
`grep -l "useMotionValue" src/components/admin/stat-card-enhanced.tsx` confirms Framer Motion animation
`grep -l "<svg" src/components/admin/stat-card-enhanced.tsx` confirms SVG sparkline (no library)
`npm run build` succeeds without errors
  </verify>
  <done>StatCardEnhanced renders animated counter, SVG sparkline, and trend badge with Framer Motion</done>
</task>

<task type="auto">
  <name>Task 3: Integrate enhanced stat cards into dashboard</name>
  <files>src/app/(dashboard)/admin/page.tsx</files>
  <action>
Update dashboard page to use new StatCardEnhanced components:

1. Replace direct Prisma queries with fetch to /api/admin/stats:
   - Use fetch with `{ next: { revalidate: 300 } }` for server-side caching
   - Parse JSON response

2. Replace StatCard imports with StatCardEnhanced:
   - Pass sparkline and change props from API response
   - Keep existing 4 cards: News Totali, News Pubblicate, Documenti, Download Totali

3. Update grid layout for responsiveness (DASH-10):
   - Already has `grid-cols-1 md:grid-cols-2 lg:grid-cols-4` - verify this is correct
   - Stats grid should be 4-col desktop, 2-col tablet, 1-col mobile

4. Keep page as server component:
   - Fetch happens server-side with caching
   - StatCardEnhanced is client component (imported dynamically if needed for bundle)

IMPORTANT: Keep recentNews and topDocuments queries - only replace the stats fetch at top.
DO NOT remove the Quick Actions section or Recent News/Top Documents sections.
  </action>
  <verify>
`npm run build` succeeds
Visit http://localhost:3017/admin - stat cards show animated numbers, sparklines visible, trend badges display
  </verify>
  <done>Dashboard displays 4 enhanced stat cards with animation, sparklines, and trend indicators; responsive grid works at all breakpoints</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run build` succeeds with no TypeScript errors
2. Dashboard loads in <500ms (check Network tab)
3. Stats cards animate on page load
4. Sparklines render as SVG (inspect element)
5. Trend badges show percentage with up/down arrows
6. Grid responsive: 4-col on lg, 2-col on md, 1-col on sm
7. Second page load faster due to 300s cache (check headers)
</verification>

<success_criteria>
- DASH-01: Stats cards show animated counters - Framer Motion counting animation
- DASH-02: Stats cards display sparklines - SVG mini charts for last 7 days
- DASH-03: Stats cards show trend indicators - Percentage change badges
- DASH-09: Next.js fetch caching - 300s revalidation on stats endpoint
- DASH-10: Responsive grid - 4/2/1 columns at lg/md/sm breakpoints
</success_criteria>

<output>
After completion, create `.planning/phases/01-dashboard-overview-enhancement/01-01-SUMMARY.md`
</output>
