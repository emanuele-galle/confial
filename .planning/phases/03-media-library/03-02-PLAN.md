---
phase: 03-media-library
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/media/media-library.tsx
  - src/components/media/media-grid.tsx
  - src/components/media/media-upload.tsx
  - src/components/media/media-sidebar.tsx
  - src/app/(dashboard)/admin/media/page.tsx
  - src/app/(dashboard)/admin/media/error.tsx
autonomous: true

must_haves:
  truths:
    - "User can view all uploaded media in virtual scrolled grid"
    - "User can upload multiple files with drag and drop"
    - "User can filter media by folder and tags"
    - "User can search media by filename"
    - "Standalone /dashboard/media page shows full media library"
  artifacts:
    - path: "src/components/media/media-library.tsx"
      provides: "Main media library container with state management"
      min_lines: 100
    - path: "src/components/media/media-grid.tsx"
      provides: "Virtual scrolled grid of media items"
      contains: "useVirtualizer"
    - path: "src/components/media/media-upload.tsx"
      provides: "Drag and drop multi-file upload zone"
      contains: "onDrop"
    - path: "src/components/media/media-sidebar.tsx"
      provides: "Folder tree and tag filter sidebar"
      min_lines: 50
    - path: "src/app/(dashboard)/admin/media/page.tsx"
      provides: "Standalone media management page"
      min_lines: 30
  key_links:
    - from: "src/components/media/media-grid.tsx"
      to: "@tanstack/react-virtual"
      via: "useVirtualizer hook for performance"
      pattern: "useVirtualizer"
    - from: "src/components/media/media-library.tsx"
      to: "/api/media/list"
      via: "SWR fetch for media items"
      pattern: "useSWR.*media/list"
    - from: "src/components/media/media-upload.tsx"
      to: "/api/media/upload"
      via: "FormData POST for file upload"
      pattern: "fetch.*media/upload"
---

<objective>
Media Library UI components with virtual scrolling, drag-drop upload, and folder/tag organization.

Purpose: Build the visual interface for media management that connects to the API layer from Plan 01. Virtual scrolling ensures performance with thousands of images.
Output: MediaLibrary component (dialog + standalone page), upload zone, filterable sidebar.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-media-library/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: MediaGrid with Virtual Scrolling</name>
  <files>
src/components/media/media-grid.tsx
src/components/media/media-item.tsx
  </files>
  <action>
**MediaGrid Component:**
- Props: { items: Media[], onSelect: (media: Media) => void, selectedId?: string, isLoading: boolean }
- Use @tanstack/react-virtual useVirtualizer for grid layout
- Configuration:
  - count: items.length
  - getScrollElement: () => containerRef.current
  - estimateSize: () => 180 (thumbnail height + padding)
  - overscan: 5 (render 5 extra rows for smooth scroll)
- Grid layout: 4 columns on lg, 3 on md, 2 on sm
- Calculate row count: Math.ceil(items.length / columns)
- Each row renders 2-4 MediaItem components

**MediaItem Component:**
- Props: { media: Media, onSelect, isSelected }
- Show thumbnail via /api/media/[id]/thumbnail?w=200&h=150
- Display filename (truncated), file size, date
- Selected state: ring-2 ring-blue-500
- Hover state: scale-105 transition
- Click triggers onSelect callback
- Checkbox overlay for multi-select mode (future)

Loading state: Show 12 skeleton cards while fetching.
Empty state: "Nessun media trovato" with upload CTA.
  </action>
  <verify>
`npx tsc --noEmit` passes.
Component renders grid with test data.
  </verify>
  <done>Virtual scrolled grid displays media items efficiently.</done>
</task>

<task type="auto">
  <name>Task 2: Upload Zone + Sidebar</name>
  <files>
src/components/media/media-upload.tsx
src/components/media/media-sidebar.tsx
  </files>
  <action>
**MediaUpload Component:**
- Drag and drop zone with react-dropzone (install if needed) OR native HTML5 drag events
- Props: { onUploadComplete: (media: Media[]) => void, folder?: string }
- Accept: image/jpeg, image/png, image/webp, image/gif
- Max file size: 10MB per file
- Multi-file support (up to 10 files at once)
- Visual states:
  - Default: dashed border, gray background, upload icon
  - Drag over: blue border, blue-50 background
  - Uploading: progress bar per file
  - Error: red border, error message
- On drop:
  - Show progress for each file (0-100%)
  - POST to /api/media/upload as FormData
  - On success: call onUploadComplete with new Media objects
  - On error: show toast with error message

**MediaSidebar Component:**
- Props: { folders: string[], tags: string[], selectedFolder, selectedTags, onFilterChange }
- Folder tree section:
  - "Tutte le immagini" (default, no filter)
  - List unique folders from media
  - Click folder to filter
  - Active folder highlighted
- Tags section:
  - Badge-style tag list
  - Click to toggle tag filter (multiple allowed)
  - Active tags have filled background
- Search input:
  - Debounced (300ms) text search
  - Searches filename and alt text
- Collapsible sections with Chevron icons
  </action>
  <verify>
`npx tsc --noEmit` passes.
Drag and drop uploads files successfully.
Sidebar filters update grid correctly.
  </verify>
  <done>Multi-file upload with progress, folder/tag filtering works.</done>
</task>

<task type="auto">
  <name>Task 3: MediaLibrary Container + Standalone Page</name>
  <files>
src/components/media/media-library.tsx
src/app/(dashboard)/admin/media/page.tsx
src/app/(dashboard)/admin/media/error.tsx
  </files>
  <action>
**MediaLibrary Component:**
- Props: { mode: 'dialog' | 'page', onSelect?: (media: Media) => void }
- State management:
  - items: Media[] from SWR
  - selectedFolder, selectedTags, searchQuery
  - selectedMedia: Media | null
  - uploadDialogOpen: boolean
- SWR fetch: `/api/media/list?page=${page}&folder=${folder}&tags=${tags}&search=${search}`
- Infinite scroll pagination: load more when reaching bottom
- Layout:
  - Dialog mode: 800x600 Dialog with grid
  - Page mode: full-width with sidebar
- Actions bar:
  - "Carica nuova immagine" button -> opens upload dialog
  - View toggle: grid/list (optional, default grid)
- On item double-click (dialog mode): call onSelect and close
- On item single-click: show selection state

**Standalone Page (/dashboard/media):**
- Server component wrapper
- Page title: "Libreria Media"
- Breadcrumb: Dashboard > Media
- Render MediaLibrary with mode="page"
- Add to admin sidebar navigation (if sidebar component exists)

**Error Boundary:**
- Standard error.tsx pattern from Phase 0
- Italian error message
- Retry button
  </action>
  <verify>
`npx tsc --noEmit` passes.
`npm run build` succeeds.
Navigate to /dashboard/media shows media library page.
  </verify>
  <done>MediaLibrary works as both dialog and standalone page with SWR data fetching.</done>
</task>

</tasks>

<verification>
All verification commands:
```bash
# TypeScript compilation
npx tsc --noEmit

# Build succeeds
npm run build
```

Manual testing:
1. Visit /dashboard/media - page loads without errors
2. Upload multiple images via drag and drop
3. Filter by folder - grid updates
4. Search by filename - results filter
5. Scroll down - virtual scroll loads more items
6. Click image - selection state shows
</verification>

<success_criteria>
- MediaGrid renders 1000+ items without performance issues (virtual scroll)
- Drag-drop upload handles multiple files with progress
- Folder and tag filtering updates grid in real-time
- Standalone /dashboard/media page accessible
- SWR handles data fetching with loading states
- TypeScript compiles, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-media-library/03-02-SUMMARY.md`
</output>
